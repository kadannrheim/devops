# Используем базовый образ для Node.js, чтобы работать с фронтендом
FROM node:18-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы фронтенда в контейнер
COPY . .

# Устанавливаем http-server для обслуживания статических файлов
RUN npm install -g http-server

# Указываем команду для запуска фронтенда
CMD ["http-server", "-p", "3000"]

# Указываем порт, который будет использоваться контейнером
EXPOSE 3000
